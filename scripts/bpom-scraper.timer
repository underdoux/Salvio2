[Unit]
Description=Run BPOM scraper daily
Requires=bpom-scraper.service

[Timer]
# Run daily at 2 AM
OnCalendar=*-*-* 02:00:00
# Add randomized delay to prevent server overload
RandomizedDelaySec=1800
# Keep track of when we last ran the service
Persistent=true

[Install]
WantedBy=timers.target
